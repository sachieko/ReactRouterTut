export interface Icontact {
  id: string;
  createdAt: number;
};

export interface IhtmlContact {
  first: string;
  last: string;
  avatar: string;
  twitter: string;
  notes: string;
  favorite: boolean;
};